{
  "type": "function",
  "function":
{
  "name": "delete_file",
  "description": "Deletes a specific file from the local file system. This action is destructive and you cannot undo it.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path of the file to be deleted."
      }
    },
    "required": ["path"]
  }
}
}
